export interface IDoneTask {
  title: string;
  description: string;
  id: number;
  date: string;
  doneDate?: string;
}

export interface ITask {
  title: string;
  description: string;
  id: number;
  date: string;
}
